<nav class="navbar navbar-expand-lg navbar-light" style="background-color: #e8e9ef;">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link px-4" href="#">Prestations</a>
            </li>
            <li class="nav-item">
                <a class="nav-link px-4" href="{{ url_for('boxesList') }}">Mes coffrets</a>
            </li>
            {% if isUserLoggedIn() %}
                <li class="nav-item">
                    <a class="nav-link px-4" href="{{ url_for('signin') }}">Se déconnecter</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link px-4" href="{{ url_for('signin') }}">Se connecter</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
